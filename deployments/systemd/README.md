# systemd

Unit-файлы для запуска `uptime-agent` на VPS как системного сервиса
(`restart=always`, env-файл с токеном и регионом). Альтернатива Docker для probe-агентов.

См. [../../apps/agent/README.md](../../apps/agent/README.md).
